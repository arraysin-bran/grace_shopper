diff a/client/store/user.js b/client/store/user.js	(rejected hunks)
@@ -24 +23 @@ const removeUser = () => ({type: REMOVE_USER})
-const updateUser = user => ({ type: UPDATE_USER, user })
+const updateUser = user => ({type: UPDATE_USER, user})
@@ -29,2 +28,2 @@ const updateUser = user => ({ type: UPDATE_USER, user })
-export const update = (user) => async dispatch => {
-  try{
+export const update = user => async dispatch => {
+  try {
@@ -37,2 +36 @@ export const update = (user) => async dispatch => {
-} 
-
+}
