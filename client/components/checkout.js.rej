diff a/client/components/checkout.js b/client/components/checkout.js	(rejected hunks)
@@ -4 +4 @@ import update from '../store/user'
-import { connect } from 'react-redux'
+import {connect} from 'react-redux'
@@ -38 +38 @@ class Checkout extends Component {
-    try{
+    try {
@@ -40,2 +40,19 @@ class Checkout extends Component {
-      const { firstName, lastName, email, streetAdress, city, state, zipCode } = this.state
-      const { data } = await axios.post(`/api/users/${userId}`, { firstName, lastName, email, streetAdress, city, state, zipCode, userId })
+      const {
+        firstName,
+        lastName,
+        email,
+        streetAdress,
+        city,
+        state,
+        zipCode
+      } = this.state
+      const {data} = await axios.post(`/api/users/${userId}`, {
+        firstName,
+        lastName,
+        email,
+        streetAdress,
+        city,
+        state,
+        zipCode,
+        userId
+      })
@@ -203 +220 @@ const mapDispatchToProps = dispatch => {
-    updateUser: (user) => dispatch(update(user))
+    updateUser: user => dispatch(update(user))
