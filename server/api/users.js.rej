diff a/server/api/users.js b/server/api/users.js	(rejected hunks)
@@ -34 +34,2 @@ router.get('/:userId', async (req, res, next) => {
-router.post('/:userId', 
+router.post(
+  '/:userId',
@@ -48,4 +49,5 @@ router.post('/:userId',
-        }, 
-        { where: 
-          {userId: inputs.id}
-      })
+        },
+        {
+          where: {userId: inputs.id}
+        }
+      )
